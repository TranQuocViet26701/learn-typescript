/* Utility Types */

// Partial<Type> ==> use to set all props to be optional

// Required<Type> ==> use to set all props to be required

// Readonly<Type> ==> use to set all props to be read-only, cannot be reassigned

// Record<Keys, Type>

// Pick<Type, Keys> ==> picking a set of keys

// Omit<Type, Keys> ==> picking all props and then remove set of keys given

// ReturnType ==> return type of function

